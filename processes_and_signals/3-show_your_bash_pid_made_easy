#!/usr/bin/env bash
#a bash script that displays process id without using ps command

   for dir_name in /proc/[0-9]*
   do
	   pid=$(basename "$dir_name")
	   name=$(cat "$dir_name/comm" 2>/dev/null)
	   if [[ $name == *bash* ]] 
	   then
           
		   echo "$pid $name"
	   fi
   done
